import React from 'react';

import Container from '../../components/container/';

const RND = (props) => {
    return (
        <Container location={props.location}>
            RND
        </Container>
    )
}

export default RND
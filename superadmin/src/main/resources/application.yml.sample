server:
    port: ${PORT:9000}

app:
  version: 1.0.19


spring:
  datasource:
    url: jdbc:mysql://localhost/tester?useSSL=false
    username: root
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create

ssh -i "anteriore.pem" ec2-user@ec2-3-14-80-255.us-east-2.compute.amazonaws.com
ssh -i "anteriore.pem" ec2-user@ec2-13-58-35-141.us-east-2.compute.amazonaws.com



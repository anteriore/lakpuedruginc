server:
    port: ${PORT:9000}

app:
  version: 1.0.19


spring:
  datasource:
    url: jdbc:mysql://localhost/tester?useSSL=false
    username: root
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update

